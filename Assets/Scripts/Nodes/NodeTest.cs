using UnityEngine;
using VN;

public class NodeTest : MonoBehaviour
{
    [SerializeField] Node m_Node;

    void Start()
    {
        // Image.Create(new Rect(0, 0, 0.5f, 0.5f), "bee", m_Node, "bee");
    }
}